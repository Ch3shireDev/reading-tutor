import {IViewService} from './IViewService';

class ViewService extends IViewService {
    constructor() {
        super()
    }

    setWordCorrect(index) {
    }

    setCurrentWordIndex(index) {
    }

    getCurrentWordIndex() {
    }
}

module.exports = {ViewService};